ext.uploadKeystoreProperties = new Properties()
File uploadKeystorePropertiesFile = rootProject.file("upload-keystore.properties")
if (uploadKeystorePropertiesFile.exists()) {
    uploadKeystoreProperties.load(new FileInputStream(uploadKeystorePropertiesFile))
}
